[Unit]
Description=GWMS Frontend

[Service]
Type=forking
ExecStart=/usr/sbin/gwms-frontend start
ExecStop=/usr/sbin/gwms-frontend stop
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
